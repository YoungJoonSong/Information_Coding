# 비즈네르 암호 (Vigenère Cipher)와 배비지의 공격법

## 1. 개요

**비즈네르 암호**는 16세기에 발명된 **다중 알파벳 치환 암호**(polyalphabetic substitution cipher)로, 수백 년 동안 **"해독 불가능"(le chiffre indéchiffrable)** 이라 불리며 안전하다고 여겨졌습니다. 그러나 19세기 **Charles Babbage**(찰스 배비지)와 **Friedrich Kasiski**(프리드리히 카시스키)에 의해 독립적으로 개발된 공격법에 의해 무너졌습니다.

- **역사적 의의**: 단일 치환 암호를 극복하려 한 획기적 시도였으나, 체계적인 수학적 공격에 의해 붕괴됨.
- **교육적 가치**: 다중 치환의 원리, 주기적 키의 약점, **빈도 분석 회피**와 **그 한계**를 이해하는 데 훌륭한 사례입니다.

---

## 2. 비즈네르 암호의 원리

### 2.1 단일 치환 vs 다중 치환

| 구분 | 단일 치환 암호 | 비즈네르 암호 (다중 치환) |
|------|----------------|---------------------------|
| **매핑** | 평문 문자 하나 → 암호문 문자 하나 (고정) | 평문 위치에 따라 **서로 다른 치환표** 사용 |
| **빈도 분석** | 취약 (E, T, A 등 빈도 노출) | **빈도가 분산**되어 단순 빈도 분석이 어려움 |
| **키** | 치환표 하나 | **키워드(keyword)** 또는 **키 스트림** |

### 2.2 암호화/복호화 규칙

- **키워드**: 예) `KEY` → 메시지 길이만큼 반복하여 `KEYKEYKEY...` 형태의 **키 스트림** 생성.
- **치환**: 각 평문 문자 \(P_i\)와 대응하는 키 문자 \(K_i\)에 대해,
  - **암호화**: \(C_i = (P_i + K_i) \bmod 26\) (덧셈)
  - **복호화**: \(P_i = (C_i - K_i) \bmod 26\) (뺄셈)

여기서 A=0, B=1, ..., Z=25로 숫자화합니다.

### 2.3 비즈네르 표(Vigenère Tableau)

전통적으로는 **26×26 표**를 사용합니다.
- 행: 평문 문자
- 열: 키 문자
- 교차점: 암호문 문자

같은 평문 문자라도 **키가 달라지면** 암호문이 달라지므로, 빈도가 여러 암호 문자에 분산됩니다.

### 2.4 예시

- **평문**: `ATTACKATDAWN`
- **키**: `LEMON` → `LEMONLEMONLE`
- **암호화**:
  - A + L → L, T + E → X, T + M → F, A + O → O, ...
  - **암호문**: `LXFOPVEFRNHR`

---

## 3. "해독 불가능"이라 불린 이유

1. **빈도 분산**: 영어의 `E`가 평문에서 12% 정도 나오더라도, 키에 따라 `E`는 I, J, M, Q 등 **서로 다른 암호 문자**로 매핑됩니다. 따라서 암호문만 보고 단순 빈도 분석을 하면 어떤 문자가 `E`인지 알기 어렵습니다.
2. **키 공간**: 키 길이가 \(k\)이면, 이론적으로 \(26^k\)가지의 키가 있어 무차별 대입(brute force)이 어렵다고 여겨졌습니다.
3. **역사적 맥락**: 16~19세기 당시 수학적·통계적 도구가 발달하지 않아, 체계적 공격법을 생각해 내기 어려웠습니다.

---

## 4. 배비지의 공격법 (카시스키 검사)

### 4.1 핵심 통찰

**같은 평문 구절이 같은 키 위치에서 암호화되면 → 같은 암호문 구절이 나타난다.**

- 평문에 `THE`가 두 번 나오고, 그때 키가 둘 다 `KEY`의 `K`를 사용하면 → 암호문에 같은 3글자 구절이 반복됩니다.
- **반복 구절 사이의 거리**는 키 길이의 **배수**일 가능성이 높습니다.

### 4.2 카시스키 검사(Kasiski Examination) 절차

1. **반복 구절 찾기**: 암호문에서 **3글자 이상** 반복되는 문자열을 찾습니다.
2. **거리 계산**: 각 반복 구절의 시작 위치 간 거리를 계산합니다.
3. **최대공약수(GCD)**: 여러 거리들의 GCD를 구합니다. 키 길이 \(k\)는 이 GCD의 **약수**일 가능성이 높습니다.
4. **키 길이 추정**: 여러 반복 쌍에서 공통으로 나타나는 키 길이 후보를 선정합니다.

### 4.3 예시 (개념)

- 암호문에서 `ABC`가 위치 5, 23, 47에 나타남.
- 거리: 23-5=18, 47-23=24, 47-5=42
- GCD(18, 24, 42) = 6 → **키 길이는 6의 약수** (1, 2, 3, 6) 후보.

### 4.4 키 길이 확정 후

키 길이 \(k\)가 알려지면:
- 암호문을 \(k\)개 열로 나눕니다 (1번째, \(k+1\)번째, ... → 1열; 2번째, \(k+2\)번째, ... → 2열; ...).
- **각 열은 단일 치환 암호**이므로, **빈도 분석**으로 각 열의 키 문자를 추정합니다.
- 영어 빈도: E, T, A, O, I, N, S, H, R, ... 순으로 자주 나옵니다.

---

## 5. 프리드만 검정 (Index of Coincidence)

**키 길이를 모를 때** 또 다른 방법으로 **일치 지수(Index of Coincidence, IC)**를 사용할 수 있습니다.

### 5.1 정의

\[
IC = \frac{\sum_{i=A}^{Z} n_i(n_i - 1)}{N(N-1)}
\]

- \(n_i\): 문자 \(i\)의 출현 횟수
- \(N\): 전체 문자 수

### 5.2 직관

- **무작위 텍스트**: IC ≈ 1/26 ≈ 0.038
- **영어 평문**: IC ≈ 0.065 ~ 0.068 (같은 문자가 더 자주 반복됨)
- **비즈네르 암호문**: 키로 \(k\)개 열로 나눈 뒤 **각 열을 독립적으로 시프트**하면, 올바른 시프트에서 **그 열의 IC가 영어에 가까워짐**

### 5.3 키 길이 추정 절차

- \(k = 1, 2, 3, ...\)에 대해 암호문을 \(k\)열로 나누고, 각 열의 IC를 구한 뒤 **평균**을 냅니다.
- 평균 IC가 **영어값(~0.065)에 가장 가까운** \(k\)가 키 길이 후보입니다.

---

## 6. 역사적 배경: 배비지 vs 카시스키

| 인물 | 시기 | 기여 |
|------|------|------|
| **Charles Babbage** | 1846년경 | 비즈네르 암호 공격법을 **독립적으로 발견**했으나 **공개하지 않음** |
| **Friedrich Kasiski** | 1863년 | 같은 방법을 **공식 출판** (카시스키 검사) |
| **William F. Friedman** | 1920년대 | **일치 지수(IC)**를 활용한 체계적 공격법 정립 |

배비지는 자서전 *Passages from the Life of a Philosopher* (1864)에서 "해독 불가능하다고 믿던 암호"를 풀었다고 기록했지만, 구체적인 방법은 발표하지 않았습니다. 오늘날 이 공격법은 **카시스키 검사**라는 이름으로 알려져 있으며, 배비지의 선행 발견도 함께 인정됩니다.

---

## 7. 실습에서 확인할 내용

1. **비즈네르 암호화/복호화**: 키워드를 이용한 암호화와 복호화 구현
2. **빈도 분산**: 같은 평문을 키를 바꿔 암호화했을 때 빈도 분포가 어떻게 달라지는지 확인
3. **카시스키 검사**: 반복 구절을 찾아 키 길이 추정
4. **일치 지수(IC)**: 여러 키 길이 후보에 대해 IC를 계산하여 키 길이 추정
5. **빈도 분석**: 키 길이를 알 때 각 열에 대한 빈도 분석으로 키 복원

---

## 8. 참고 문헌 및 링크

- Simon Singh, *The Code Book* (암호학 입문서)
- Kahn, D., *The Codebreakers*
- Wikipedia: [Vigenère cipher](https://en.wikipedia.org/wiki/Vigen%C3%A8re_cipher), [Kasiski examination](https://en.wikipedia.org/wiki/Kasiski_examination)
- University of St Andrews: [Charles Babbage and deciphering codes](https://mathshistory.st-andrews.ac.uk/Extras/Babbage_deciphering/)

이 자료와 함께 제공되는 `vigenere_cipher.py` 실습 프로그램으로 위 내용을 직접 구현하고 실험해 볼 수 있습니다.
